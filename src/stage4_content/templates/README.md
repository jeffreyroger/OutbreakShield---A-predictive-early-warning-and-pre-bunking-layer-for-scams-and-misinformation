# Content templates

One file per `(technique, language)` pair, e.g. `authority_impersonation.hi.yaml`.
Each template supplies a hand-written, native-speaker-reviewed `technique_layer`
and a model-fillable `variant_layer` slot. These are the quality floor (DR-5) —
build them before touching the generation model (Phase 5, Step 5.1).
